%div.box
  .links
    %span.add
      = link_to 'New product', new_product_path
  %h2 Listing products
  %p.description Lorem ipsum dolor sit amet    

  %table
    %tr
      %th Name
      %th Price
      %th Product category
      %th Inventory
      %th Published

    - @products.each do |product|
      %tr
        %td= link_to product.name, product
        %td= h product.price 
        %td= link_to product.product_category, product.product_category if product.product_category
        %td= h product.inventory
        %td= product.published ? 'yes' : 'no'
        %td= image_tag product.picture(:thumb)
        %td= link_to 'Edit', edit_product_path(product) 
        %td= link_to 'Destroy', product, :confirm => 'Are you sure?', :method => :delete 
- content_for :subheader do
  %b Products
  %br
  This page allows you administer products
